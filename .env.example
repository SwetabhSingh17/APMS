# ==============================================================================
# APMS (Academic Project Management System) — Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ==============================================================================

# --- Database ---
# PostgreSQL connection string (required)
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/integral_project_hub

# --- Server ---
# Port the server will listen on (default: 3000)
PORT=3000

# Environment mode: "development" or "production"
NODE_ENV=development

# --- Session ---
# Secret key for signing session cookies (use a strong random string in production)
SESSION_SECRET=change-me-to-a-secure-random-string
